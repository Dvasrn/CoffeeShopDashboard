export default function Home() {
  return (
    <div className="flex justify-center items-center h-full w-full">
      check if user logged in if logged in then go to dashboard if not then go
      to login
    </div>
  );
}
